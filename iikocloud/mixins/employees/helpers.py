"""Employees helpers mixin — convenience-методы (заготовка)."""

from iikocloud.mixins.employees.core import EmployeesCoreMixin


class EmployeesHelpersMixin(EmployeesCoreMixin):
    """Публичный employees mixin (helpers появятся позже)."""
