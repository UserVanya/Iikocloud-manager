"""Orders helpers mixin — convenience-методы (заготовка)."""

from iikocloud.mixins.orders.core import OrdersCoreMixin


class OrdersHelpersMixin(OrdersCoreMixin):
    """Публичный orders mixin (helpers появятся позже)."""
