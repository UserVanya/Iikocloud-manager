"""Banquets & Reserves helpers mixin — convenience-методы (заготовка)."""

from iikocloud.mixins.banquets.core import BanquetsCoreMixin


class BanquetsHelpersMixin(BanquetsCoreMixin):
    """Публичный banquets mixin (helpers появятся позже)."""
